syntax = "proto3";
package market.portal;

import "gogoproto/gogo.proto";

option go_package = "market/x/portal/types";

// Params defines the parameters for the module.
message Params {
  option (gogoproto.goproto_stringer) = false;
  
  // Onomy channel
  string onomy_channel = 1;
  
  // Reserve channel
  string reserve_channel = 2;

}
